# 1testweb

Static site deployed via Coolify. Content is edited through a Telegram → Claude managed agent bridge running on the same host.

## Local preview

```
python3 -m http.server -d . 8001
```

Then open http://localhost:8001.
